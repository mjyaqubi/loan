export enum SERVICE_CONFIG {
  NAME = 'service.name',
  DESCRIPTION = 'service.description',
  VERSION = 'service.version',
}

export enum SERVICE_URL_CONFIG {
  BASE = 'service.url.base',
  DOCS = 'service.url.docs',
}

export enum SERVER_CONFIG {
  HOST = 'server.host',
  PORT = 'server.port',
}

export enum SECURITY_DOCS_CONFIG {
  USERNAME = 'security.docs.username',
  PASSWORD = 'security.docs.password',
}
